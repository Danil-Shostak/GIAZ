<ContentDialog
    x:Class="EducationInstitutionsRB.Views.DistrictDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    PrimaryButtonText="Сохранить"
    CloseButtonText="Отмена"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    DefaultButton="Primary"
    mc:Ignorable="d">

    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Padding="0,0,8,0">
            <TextBlock x:Name="DialogTitle" 
                       Text="Добавить район" 
                       FontSize="18" 
                       FontWeight="SemiBold"/>

            <TextBlock Text="Поля, отмеченные *, обязательны для заполнения" 
                       FontStyle="Italic" Foreground="Gray" FontSize="12"/>

            <TextBox Header="Название района *" 
                     Text="{x:Bind District.Name, Mode=TwoWay}"
                     PlaceholderText="Введите название района"
                     HorizontalAlignment="Stretch"/>

            <ComboBox Header="Область *" 
                      ItemsSource="{x:Bind Regions}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{x:Bind District.RegionId, Mode=TwoWay}"
                      PlaceholderText="Выберите область"
                      HorizontalAlignment="Stretch"/>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>