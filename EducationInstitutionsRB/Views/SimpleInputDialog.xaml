<ContentDialog
    x:Class="EducationInstitutionsRB.Views.SimpleInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Bind Title}"
    PrimaryButtonText="Сохранить"
    CloseButtonText="Отмена"
    DefaultButton="Primary"
    mc:Ignorable="d">

    <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Padding="0,0,8,0">
            <TextBlock Text="{x:Bind Message}" TextWrapping="Wrap" FontSize="14"/>
            <TextBox Text="{x:Bind InputText, Mode=TwoWay}" 
                     PlaceholderText="Введите значение..."
                     HorizontalAlignment="Stretch"
                     FontSize="14"/>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>