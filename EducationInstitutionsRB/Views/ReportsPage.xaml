<Page
    x:Class="EducationInstitutionsRB.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:EducationInstitutionsRB.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToYesNoConverter x:Key="BooleanToYesNoConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,20">
            <TextBlock Text="Система отчётов" FontSize="28" FontWeight="Bold" 
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            <TextBlock x:Name="ReportInfoText" Text="Выберите параметры для генерации отчёта" 
                       FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Панель управления -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" Padding="16" Margin="0,0,0,16"
                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <StackPanel Spacing="16">
                <!-- Первая строка - выбор области и района -->
                <StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
                    <!-- Выбор региона -->
                    <StackPanel Spacing="6">
                        <TextBlock Text="Область:" FontWeight="SemiBold" FontSize="14"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <ComboBox x:Name="RegionCombo" Width="220" Height="36"
                                      DisplayMemberPath="Name"/>
                            <Button Content="✕" x:Name="ClearRegionButton"
                                    Click="ClearRegionButton_Click"
                                    Width="28" Height="28"
                                    FontSize="14" FontWeight="Bold"
                                    ToolTipService.ToolTip="Очистить область"
                                    Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Выбор района -->
                    <StackPanel Spacing="6">
                        <TextBlock Text="Район:" FontWeight="SemiBold" FontSize="14"/>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <ComboBox x:Name="DistrictCombo" Width="220" Height="36"
                                      DisplayMemberPath="Name"
                                      IsEnabled="False"/>
                            <Button Content="✕" x:Name="ClearDistrictButton"
                                    Click="ClearDistrictButton_Click"
                                    Width="28" Height="28"
                                    FontSize="14" FontWeight="Bold"
                                    ToolTipService.ToolTip="Очистить район"
                                    Visibility="Collapsed"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Чекбокс для всей республики -->
                    <StackPanel Spacing="6" VerticalAlignment="Bottom" Margin="20,0,0,0">
                        <CheckBox x:Name="AllRepublicCheckBox" 
                                  Content="Вся Республика Беларусь"
                                  Checked="AllRepublicCheckBox_Checked"
                                  Unchecked="AllRepublicCheckBox_Unchecked"/>
                    </StackPanel>
                </StackPanel>

                <!-- Вторая строка - тип отчета и кнопки -->
                <StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
                    <!-- Тип отчета -->
                    <StackPanel Spacing="6">
                        <TextBlock Text="Тип отчёта:" FontWeight="SemiBold" FontSize="14"/>
                        <ComboBox x:Name="ReportTypeCombo" Width="240" Height="36"
                                  SelectionChanged="ReportTypeCombo_SelectionChanged">
                            <x:String>📊 Основной отчёт</x:String>
                            <x:String>🏫 Детальная информация</x:String>
                            <x:String>💻 Инфраструктура</x:String>
                            <x:String>👥 Персонал</x:String>
                        </ComboBox>
                    </StackPanel>

                    <!-- Кнопка генерации -->
                    <StackPanel Spacing="6" VerticalAlignment="Bottom" Margin="20,0,0,0">
                        <TextBlock Text="Действия:" FontWeight="SemiBold" FontSize="14" Opacity="0"/>
                        <Button Content="📊 Сгенерировать отчёт" x:Name="GenerateButton"
                                Click="GenerateButton_Click"
                                Style="{ThemeResource AccentButtonStyle}"
                                Height="36" MinWidth="160"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Результаты отчета -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" Padding="0"
                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid>
                <!-- Индикатор загрузки -->
                <ProgressRing x:Name="LoadingProgress" 
                              IsActive="False"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Width="40" Height="40"/>

                <!-- Контент отчета -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" x:Name="ReportScrollViewer">
                    <StackPanel>
                        <!-- 📊 ОСНОВНОЙ ОТЧЕТ -->
                        <StackPanel x:Name="BasicReportPanel" Visibility="Visible">
                            <!-- Заголовок таблицы -->
                            <Grid Background="{ThemeResource SubtleFillColorSecondaryBrush}" 
                                  Padding="20,14" 
                                  BorderThickness="0,0,0,1" 
                                  BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                                    <ColumnDefinition Width="Auto" MinWidth="130"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Учреждение" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="1" Text="Тип" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="2" Text="Учащиеся" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="3" Text="Преподаватели" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="4" Text="Соотношение" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="5" Text="Площадь (м²)" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>

                            <!-- Данные отчета -->
                            <ListView x:Name="BasicReportListView" 
                                      SelectionMode="None"
                                      IsItemClickEnabled="False">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="20,12" 
                                              BorderThickness="0,0,0,1" 
                                              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                                <ColumnDefinition Width="Auto" MinWidth="150"/>
                                                <ColumnDefinition Width="Auto" MinWidth="130"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                       VerticalAlignment="Center" 
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="1" Text="{Binding Type}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="2" Text="{Binding StudentCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="3" Text="{Binding TeacherCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="4" Text="{Binding TeacherStudentRatio}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Foreground="{ThemeResource SystemAccentColor}"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="5" Text="{Binding TotalArea}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <!-- Итоги -->
                            <Border Background="{ThemeResource SubtleFillColorTertiaryBrush}" 
                                    Padding="20,14" 
                                    BorderThickness="0,1,0,0" 
                                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid ColumnDefinitions="*,Auto,140,150,130,120">
                                    <TextBlock Grid.Column="0" Text="Итого:" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="1" Text="" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="2" x:Name="TotalStudentsText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="3" x:Name="TotalTeachersText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="4" x:Name="TotalRatioText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="5" x:Name="TotalAreaText" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <!-- 🏫 ДЕТАЛЬНАЯ ИНФОРМАЦИЯ -->
                        <StackPanel x:Name="DetailedReportPanel" Visibility="Collapsed">
                            <Grid Background="{ThemeResource SubtleFillColorSecondaryBrush}" 
                                  Padding="20,14" 
                                  BorderThickness="0,0,0,1" 
                                  BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="180"/>
                                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                                    <ColumnDefinition Width="Auto" MinWidth="130"/>
                                    <ColumnDefinition Width="Auto" MinWidth="160"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Учреждение" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="1" Text="Адрес" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="2" Text="Директор" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="3" Text="Телефон" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="4" Text="Email" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="5" Text="Статус" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="6" Text="Год осн." FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>

                            <ListView x:Name="DetailedReportListView" 
                                      SelectionMode="None"
                                      IsItemClickEnabled="False">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="20,12" 
                                              BorderThickness="0,0,0,1" 
                                              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto" MinWidth="180"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                                <ColumnDefinition Width="Auto" MinWidth="130"/>
                                                <ColumnDefinition Width="Auto" MinWidth="160"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                       VerticalAlignment="Center" 
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="1" Text="{Binding Address}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="2" Text="{Binding DirectorName}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="3" Text="{Binding Contacts}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="4" Text="{Binding Email}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="5" Text="{Binding Status}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="6" Text="{Binding FoundationYear}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <!-- 💻 ИНФРАСТРУКТУРА -->
                        <StackPanel x:Name="InfrastructureReportPanel" Visibility="Collapsed">
                            <Grid Background="{ThemeResource SubtleFillColorSecondaryBrush}" 
                                  Padding="20,14" 
                                  BorderThickness="0,0,0,1" 
                                  BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Учреждение" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="1" Text="Спортзал" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="2" Text="Столовая" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="3" Text="Библиотека" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="4" Text="Компьютеры" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="5" Text="Кабинеты" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="6" Text="Площадь (м²)" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="7" Text="Категория" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                            </Grid>

                            <ListView x:Name="InfrastructureReportListView" 
                                      SelectionMode="None"
                                      IsItemClickEnabled="False">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="20,12" 
                                              BorderThickness="0,0,0,1" 
                                              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                       VerticalAlignment="Center" 
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="1" Text="{Binding HasSportsHall, Converter={StaticResource BooleanToYesNoConverter}}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="2" Text="{Binding HasDiningRoom, Converter={StaticResource BooleanToYesNoConverter}}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="3" Text="{Binding HasLibrary, Converter={StaticResource BooleanToYesNoConverter}}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="4" Text="{Binding ComputerCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="5" Text="{Binding ClassroomCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="6" Text="{Binding TotalArea}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="7" Text="{Binding AccreditationCategory}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <!-- 👥 ПЕРСОНАЛ -->
                        <StackPanel x:Name="StaffReportPanel" Visibility="Collapsed">
                            <Grid Background="{ThemeResource SubtleFillColorSecondaryBrush}" 
                                  Padding="20,14" 
                                  BorderThickness="0,0,0,1" 
                                  BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                                    <ColumnDefinition Width="Auto" MinWidth="160"/>
                                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                                    <ColumnDefinition Width="Auto" MinWidth="140"/>
                                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Учреждение" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="1" Text="Преподаватели" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="2" Text="Админ. персонал" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="3" Text="Всего персонала" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="4" Text="Принято" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="5" Text="Отчислено" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                <TextBlock Grid.Column="6" Text="Соотношение" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>

                            <ListView x:Name="StaffReportListView" 
                                      SelectionMode="None"
                                      IsItemClickEnabled="False">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="20,12" 
                                              BorderThickness="0,0,0,1" 
                                              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                                <ColumnDefinition Width="Auto" MinWidth="160"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                                <ColumnDefinition Width="Auto" MinWidth="140"/>
                                                <ColumnDefinition Width="Auto" MinWidth="120"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                       VerticalAlignment="Center" 
                                                       TextWrapping="WrapWholeWords"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="1" Text="{Binding TeacherCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="2" Text="{Binding AdministrativeStaffCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="3" Text="{Binding StaffCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="4" Text="{Binding AdmittedCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="5" Text="{Binding ExpelledCount}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Margin="0,0,20,0"/>

                                            <TextBlock Grid.Column="6" Text="{Binding TeacherStudentRatio}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Right"
                                                       Foreground="{ThemeResource SystemAccentColor}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <!-- Итоги для отчета персонала -->
                            <Border Background="{ThemeResource SubtleFillColorTertiaryBrush}" 
                                    Padding="20,14" 
                                    BorderThickness="0,1,0,0" 
                                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                                <Grid ColumnDefinitions="*,120,140,160,140,140,120">
                                    <TextBlock Grid.Column="0" Text="Итого:" FontWeight="SemiBold" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="1" x:Name="TotalTeachersStaffText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="2" x:Name="TotalAdminStaffText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="3" x:Name="TotalAllStaffText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="4" x:Name="TotalAdmittedText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="5" x:Name="TotalExpelledText" FontWeight="SemiBold" HorizontalAlignment="Right" Margin="0,0,20,0"/>
                                    <TextBlock Grid.Column="6" Text="—" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!-- Сообщение при отсутствии данных -->
                <Border x:Name="NoDataMessage" Background="Transparent" 
                        Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Spacing="12" HorizontalAlignment="Center">
                        <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="NoDataTitleText" Text="Нет данных для отображения" 
                                   FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="NoDataMessageText" Text="Выберите параметры и сгенерируйте отчёт" 
                                   FontSize="14" HorizontalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>