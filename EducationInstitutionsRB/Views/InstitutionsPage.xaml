<Page
    x:Class="EducationInstitutionsRB.Views.InstitutionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:EducationInstitutionsRB.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:FormatConverter x:Key="FormatConverter"/>
        <converters:IntToStringConverter x:Key="IntToStringConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель поиска -->
        <StackPanel Grid.Row="0" Padding="20" Spacing="10">
            <TextBlock Text="Поиск учреждений" FontSize="18" FontWeight="Bold"/>
            <TextBox PlaceholderText="Поиск по названию..." x:Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged"/>

            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Bottom">
                <ComboBox Header="Область" Width="200" x:Name="RegionCombo"
                          DisplayMemberPath="Name"
                          SelectionChanged="RegionCombo_SelectionChanged"
                          VerticalAlignment="Bottom"/>

                <ComboBox Header="Район" Width="200" x:Name="DistrictCombo"
                          DisplayMemberPath="Name"
                          SelectionChanged="DistrictCombo_SelectionChanged"
                          VerticalAlignment="Bottom"/>

                <ComboBox Header="Тип" Width="150" x:Name="TypeCombo"
                          SelectionChanged="TypeCombo_SelectionChanged"
                          VerticalAlignment="Bottom">
                    <x:String>Школа</x:String>
                    <x:String>Гимназия</x:String>
                    <x:String>Лицей</x:String>
                    <x:String>Колледж</x:String>
                    <x:String>Университет</x:String>
                </ComboBox>

                <Button Content="➕ Добавить учреждение" Click="AddInstitutionButton_Click"
                        Background="#0078D4" Foreground="White" Padding="5"
                        VerticalAlignment="Bottom"
                        Height="32"
                        FontSize="15"/>
            </StackPanel>
        </StackPanel>

        <!-- Список учреждений -->
        <!-- EducationInstitutionsRB/Views/InstitutionsPage.xaml - ОБНОВЛЯЕМ ListView -->
        <ListView
    Grid.Row="1"
    x:Name="InstitutionsList"
    SelectionMode="Single"
    IsItemClickEnabled="True"
    ItemClick="InstitutionsList_ItemClick">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#E1E1E1" BorderThickness="0,0,0,1" Padding="0,8">
                        <Grid ColumnDefinitions="*,Auto" Margin="10,5">
                            <!-- Информация об учреждении -->
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Type}" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding Address}" FontSize="12"/>
                                <TextBlock Text="{Binding District.Region.Name}" FontSize="11" Foreground="DarkGray"/>

                                <!-- Расширенная информация в списке -->
                                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
                                    <TextBlock FontSize="11" Foreground="#0078D4">
                                <Run Text="Учеников: "/>
                                <Run Text="{Binding StudentCount}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="#0078D4">
                                <Run Text="Педагогов: "/>
                                <Run Text="{Binding TeacherCount}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="#0078D4">
                                <Run Text="Категория: "/>
                                <Run Text="{Binding AccreditationCategory}"/>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>

                            <!-- Кнопки действий -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Button Content="✏️" Click="EditButton_Click" 
                                Tag="{Binding}"
                                ToolTipService.ToolTip="Редактировать"
                                Background="Transparent" BorderThickness="0"
                                Padding="8,4" FontSize="12"/>

                                <Button Content="🗑️" Click="DeleteButton_Click" 
                                Tag="{Binding}"
                                ToolTipService.ToolTip="Удалить"
                                Background="Transparent" BorderThickness="0"
                                Padding="8,4" FontSize="12"
                                Foreground="#D13438"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>